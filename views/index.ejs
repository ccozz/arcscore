<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>ARC Score</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="/style.css" />
  <script>
    document.documentElement.classList.add('dark-mode');
  </script>
</head>
<body>
  <%- include('partials/navbar') %>
  
  <div class="d-flex">
    <button id="modoToggle" class="btn btn-sm btn-outline-light ms-auto m-3">
      🌙 Modo claro
    </button> 
  </div>

  <main class="container text-center mt-5">
    <h1 class="display-4">Bienvenido a 🥅 ARC Score ⚽</h1>
    <p class="lead">Organizá, jugá y disfrutá el torneo como nunca antes.</p>
  </main>

  <script>
    const toggle = document.getElementById('modoToggle');
    const body = document.body;

    // Activar modo oscuro por defecto
    body.classList.add('dark-mode');
    toggle.innerHTML = '🌙 Modo claro';

    toggle.addEventListener('click', () => {
      body.classList.toggle('dark-mode');

      // Cambiar ícono y texto
      if (body.classList.contains('dark-mode')) {
        toggle.innerHTML = '🌙 Modo claro';
      } else {
        toggle.innerHTML = '🌞 Modo oscuro';
      }

      // Cambiar clase del botón
      toggle.classList.toggle('btn-outline-light');
      toggle.classList.toggle('btn-outline-dark');
    });
  </script>

</body>
</html>